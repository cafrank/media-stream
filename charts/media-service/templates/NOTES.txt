media-service {{ include "media-service.image" . }} deployed to namespace {{ .Release.Namespace }}.

Service: {{ include "media-service.serviceName" . }}:{{ .Values.service.port }}
MongoDB: {{ if .Values.mongodb.enabled }}in-chart ({{ include "media-service.mongodb.fullname" . }}){{ else }}external{{ end }}

{{- if .Values.ingress.enabled }}
Ingress:
{{- range .Values.ingress.hosts }}
{{- $host := .host }}
{{- range .paths }}
  http{{ if $.Values.ingress.tls }}s{{ end }}://{{ $host }}{{ .path }}
{{- end }}
{{- end }}
{{- else if eq .Values.service.type "NodePort" }}
NodePort:
  kubectl -n {{ .Release.Namespace }} get svc {{ include "media-service.serviceName" . }} -o jsonpath='{.spec.ports[0].nodePort}'
{{- else }}
Port-forward:
  kubectl -n {{ .Release.Namespace }} port-forward svc/{{ include "media-service.serviceName" . }} 8080:{{ .Values.service.port }}
  curl http://localhost:8080/api/media
{{- end }}
